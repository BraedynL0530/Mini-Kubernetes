# choice for devlog to remember

7/17 1:15 am, i got lazy so instead of a single meowlet(kublet) binary with diffrent configs there gonna be seperate. 

# notes for fuctionality
check heart beat every 10 seconds, after 40 seconds no response its dead, saved in tui and stuff though, after 5 minutes(300 seconds) its deleted from cache/json or whatever i end up using, and it can no longer reclaim its spot and will be remade or replaced or whatever